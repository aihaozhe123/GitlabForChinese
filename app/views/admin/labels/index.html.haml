- page_title "标签"

%div
  = link_to new_admin_label_path, class: "pull-right btn btn-nr btn-new" do
    新标签
  %h3.page-title
    标签
%hr

.labels
  - if @labels.present?
    %ul.bordered-list.manage-labels-list
      = render @labels
    = paginate @labels, theme: 'gitlab'
  - else
    .light-well
      .nothing-here-block 还没有标签
