.row.empty-state.labels
  .pull-right.col-xs-12.col-sm-6
    .svg-content
      = render 'shared/empty_states/icons/labels.svg'
  .col-xs-12.col-sm-6
    .text-content
      %h4 标签可以应用于问题和合并请求以对其进行分类。
      %p 您也可以将星号标签设为优先标记。
      - if can?(current_user, :admin_label, @project)
        = link_to '新标签', new_namespace_project_label_path(@project.namespace, @project), class: 'btn btn-new', title: '新标签', id: 'new_label_link'
        = link_to '生成默认的标签集', generate_namespace_project_labels_path(@project.namespace, @project), method: :post, class: 'btn btn-success btn-inverted', title: '生成默认的标签集', id: 'generate_labels_link'
