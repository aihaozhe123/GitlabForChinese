= f.hidden_field(:name)

= dropdown_tag('选择标记或创建通配符',
               options: { toggle_class: 'js-protected-tag-select js-filter-submit wide',
                          filter: true, dropdown_class: "dropdown-menu-selectable", placeholder: "搜索受保护的标记",
                          footer_content: true,
                          data: { show_no: true, show_any: true, show_upcoming: true,
                                  selected: params[:protected_tag_name],
                                  project_id: @project.try(:id) } }) do

  %ul.dropdown-footer-list
    %li
      = link_to '#', title: "新保护标记", class: "create-new-protected-tag" do
        创建通配符
        %code
